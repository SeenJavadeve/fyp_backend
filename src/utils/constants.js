const DB_NAME = 'fyp_db';
const API_VERSION_PREFIX = "/api/v1";

// Customer types for invoices
const customer_type = ['account', 'walkin'];

// Invoice status values
const invoice_status = ['pending', 'complete'];

export { DB_NAME, API_VERSION_PREFIX, customer_type, invoice_status };
